{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && PORT=${PORT:-3000} npm start",
    "restartPolicyType": "NEVER"
  }
}
